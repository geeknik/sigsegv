# Usurper Reborn — Nginx Reverse Proxy Container
# Handles HTTP routing, WebSocket upgrade, and optional SSL termination

FROM nginx:alpine
COPY docker/nginx/nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
